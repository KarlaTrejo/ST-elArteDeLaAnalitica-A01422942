INSTRUCCIONES

El archivo llamado "Actividad_2.py" debe correrse ya sea con un IDE que pueda procesar el lenguaje de python
por ejemplo Thonny, Viaul Studio Code con las extensiones adecuadas, pycharm, entre otros.

Este archivo debe acceder a un archivo de la carpeta "Car accidents", llamado "data.csv" que debe estar 
localizada en el mismo carpeta que este archivo por la configuración que este tiene. De cambiarse de 
lugar este archivo, el programa no podrá cometer su función.

El usuario no debe interactuar con el archivo, solamente debe correrlo para que este realice lo que se indica
en el mismo.