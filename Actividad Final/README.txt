INSTRUCCIONES
Programas realizados por Karla Trejo

El archivo llamado "Actividad_final.py" debe correrse ya sea con la terminal tomando en cuenta que tienes que descargar el lenguaje con una versión reciente o puede ser con un IDE que pueda procesar el lenguaje de python por ejemplo Thonny, Visual Studio Code con las extensiones adecuadas, pycharm, entre otros.

Además del IDE, debe considerarse la instalación de las extensiones necesarias, como pandas, numpy, seaborn y matplotlib, que son de las que se realizan importaciones dentro del programa.

Este archivo debe acceder a un archivo de la carpeta "Car accidents", llamado "data.csv" que debe estar localizada en el mismo carpeta que este archivo por la configuración que este tiene. De cambiarse de lugar este archivo, el programa no podrá cometer su función.

El usuario no debe interactuar con el programa con tal, solamente debe correrlo para que este realice lo que se indica en él mismo en su documentación.

Solamente debe considerarse que para que se vean cada una de las visualizaciones de datos (cada histograma, diagrama de caja y bigotes y mapa de calor), se abre una ventana adicional, la cual tiene que cerrar para ver la siguiente en caso de haber una siguiente visualización en el programa.