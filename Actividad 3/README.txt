INSTRUCCIONES

El archivo llamado "Actividad_3.py" debe correrse ya sea con un IDE que pueda procesar el lenguaje de python por ejemplo Thonny, Viaul Studio Code con las extensiones adecuadas, pycharm, entre otros, en general además del IDE debe considerarse la instalación de las extensiones necesarias, como pandas, Matplot, entre las otras que podremos apreciar al momento de abrir el programa y ver las importaciones.

Este archivo debe acceder a un archivo de la carpeta "Car accidents", llamado "data.csv" que debe estar localizada en el mismo carpeta que este archivo por la configuración que este tiene. De cambiarse de lugar este archivo, el programa no podrá cometer su función.

El usuario no debe interactuar con el archivo, solamente debe correrlo para que este realice lo que se indica en él mismo.

Solamente debe considerarse que para que se vean cada una de las visualizaciones de datos, se abre una ventana adicional, la cual tiene que cerrar para ver la siguiente en caso de haber una siguiente.